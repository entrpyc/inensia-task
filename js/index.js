function swapHtmlTagsInDOMElementFactory(DOMElement) {
  // get DOM element
  
  // find paragraphs and spans
  const paragraphs = DOMElement.getElementsByTagName('p')
  const spans = DOMElement.getElementsByTagName('span')

  debugger

  // swap tag names

  // update DOM
  
  // return array of new tags
}