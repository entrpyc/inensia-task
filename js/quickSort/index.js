export function quickSort(arr) {
  return arr
}

// pick pivot

// create partition resolver

// iterate over partition

// if value at index is > than pivot value, swap them

// move pivot

// recursively call the partition resolver on both sides of the pivot